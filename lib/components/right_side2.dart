import 'package:flutter/material.dart';

class RightSide2 extends StatelessWidget {
  const RightSide2({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 300,
      child: Card(
        child: Text('RightSide2'),
      ),
    );
  }
}
